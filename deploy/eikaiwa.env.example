# EC2 上の環境変数ファイル
# このファイルを /etc/eikaiwa.env としてコピーし、値を実際のものに変更する
# sudo cp eikaiwa.env.example /etc/eikaiwa.env
# sudo chmod 600 /etc/eikaiwa.env

SECRET_KEY=your-very-long-random-secret-key-here
DEBUG=False

# nicelinks の CloudFront に相乗りする場合
ALLOWED_HOSTS=api.nicelinks.net,nicelinks.net
DATABASE_URL=postgresql://user:password@your-rds-endpoint:5432/eikaiwa
OPENAI_API_KEY=sk-xxxxxxxxxxxx

# CORS: iOSアプリはブラウザではないので実質不要だが念のため設定
CORS_ALLOW_ALL_ORIGINS=True

# CloudFront 相乗り時のプレフィックス設定
FORCE_SCRIPT_NAME=/eikaiwa
STATIC_URL=/eikaiwa-static/
